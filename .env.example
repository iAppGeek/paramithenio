# Root-level env — scripts and CI only
# These values must NEVER reach a browser or mobile bundle
# Copy to .env and fill in your project values

# Used by scripts/add-story.ts and supabase CLI migrations
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
